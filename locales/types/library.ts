import { MetaData } from "./meta"

type librarycard = {
  titleOne: string
  titleTwo: string
  infoEmail: string
  pressEmail: string
  titleThree: string
}

export type LibraryData = {
  metaData: MetaData
}
